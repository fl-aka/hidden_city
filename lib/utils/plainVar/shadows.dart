import 'package:flutter/material.dart';

final List<Shadow> shiro = [
  const Shadow(offset: Offset(-1, -1), color: Colors.white, blurRadius: 2),
  const Shadow(offset: Offset(1, -1), color: Colors.white, blurRadius: 2),
  const Shadow(offset: Offset(1, 1), color: Colors.white, blurRadius: 2),
  const Shadow(offset: Offset(-1, 1), color: Colors.white, blurRadius: 2),
];
